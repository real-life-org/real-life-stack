name: Publish Packages

on:
  # DER REGELWEG, seit release-please mit RELEASE_PLEASE_TOKEN laeuft: dessen
  # Releases tragen einen echten Benutzer-Token, also feuert `published` ganz
  # normal. Frueher stand hier das Gegenteil, und es stimmte auch — unter
  # GITHUB_TOKEN loest GitHub keine Workflows aus. Seit dem 10.09.2026 nicht
  # mehr.
  #
  # npms Trusted Publishing bindet auf den Top-Level-Workflow, und das ist
  # publish.yml auf diesem Weg genauso wie per Dispatch. Nachgewiesen am
  # 15.09.2026 (Lauf 35011958238, veroeffentlicht samt Provenance).
  #
  # Der workflow_dispatch darunter bleibt der Ersatzweg fuer den Fall ohne
  # Secret; release-please stoesst ihn dann an. Dass nie beide Wege gleichzeitig
  # laufen, regelt die Bedingung an den `trigger-*`-Jobs in release-please.yml —
  # sonst trifft der zweite Lauf auf eine bereits veroeffentlichte Version und
  # npm antwortet mit `E409 — Cannot publish over previously staged version`.
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release-Tag, an das die tgz-Assets gehängt werden'
        required: true

jobs:
  # Tag-Klassifikation als EIGENER Job (Job-level if ueberspringt den Publish
  # sauber, ein Step-exit liesse Folge-Steps mit leerem dir weiterlaufen).
  classify:
    runs-on: ubuntu-latest
    outputs:
      kind: ${{ steps.c.outputs.kind }}
      dir: ${{ steps.c.outputs.dir }}
    steps:
      - id: c
        env:
          TAG: ${{ github.event_name == 'release' && github.event.release.tag_name || inputs.tag }}
        run: |
          set -euo pipefail
          case "$TAG" in
            data-interface-v*)    kind=package; dir=packages/data-interface ;;
            toolkit-v*)           kind=package; dir=packages/toolkit ;;
            mock-connector-v*)    kind=package; dir=packages/mock-connector ;;
            local-connector-v*)   kind=package; dir=packages/local-connector ;;
            wot-connector-v*)     kind=package; dir=packages/wot-connector ;;
            supabase-connector-v*) kind=package; dir=packages/supabase-connector ;;
            app-v*)
              # App-Releases sind eine ERWARTETE Tag-Klasse, aber kein npm-Paket.
              echo "App-Release-Tag '$TAG' — kein npm-Publish noetig."
              kind=app; dir= ;;
            *)
              echo "ABBRUCH: Tag '$TAG' gehoert zu keinem publizierbaren Paket." >&2
              echo "Erwartet: <paket>-v* (oder app-v* als Skip)." >&2
              exit 1 ;;
          esac
          echo "kind=$kind" >> "$GITHUB_OUTPUT"
          echo "dir=$dir"   >> "$GITHUB_OUTPUT"

  publish:
    needs: classify
    if: needs.classify.outputs.kind == 'package'
    runs-on: ubuntu-latest
    # Zwei Ziele, bewusst parallel: tgz-Assets am GitHub-Release UND npm via
    # Trusted Publishing (OIDC). Erst die Assets, dann npm — scheitert npm, bleibt
    # der Tarball intakt und der Lauf trotzdem rot.
    permissions:
      contents: write
      # Ohne id-token kein OIDC-Token und damit kein Trusted Publishing.
      id-token: write
    steps:
      - uses: actions/checkout@v4
        with:
          # Beim manuellen Re-Run MUSS der Checkout auf dem Ziel-Tag stehen —
          # sonst wuerden Tarballs vom master-HEAD mit --clobber die Assets eines
          # aelteren Releases ueberschreiben.
          ref: ${{ github.event_name == 'release' && github.event.release.tag_name || inputs.tag }}
          # Kein Git-Token im Worktree persistieren, waehrend gleich fremder
          # Dependency-Code (pnpm install) laeuft. Upload nutzt gh + GH_TOKEN.
          persist-credentials: false

      # PROVENIENZ FAIL-CLOSED, bevor irgendetwas installiert/gepackt/publiziert
      # wird: npm-Publishes sind irreversibel. Der Tag muss beweisbar zu genau dem
      # Paket + der Version gehoeren, die am ausgecheckten Ref stehen.
      - name: Tag ↔ Paket + Version binden
        env:
          TAG: ${{ github.event_name == 'release' && github.event.release.tag_name || inputs.tag }}
          DIR: ${{ needs.classify.outputs.dir }}
        run: |
          set -euo pipefail
          # 1. Exakt <component>-v<SemVer>.
          if [[ ! "$TAG" =~ ^([a-z0-9-]+)-v([0-9]+\.[0-9]+\.[0-9]+([-+][0-9A-Za-z.-]+)*)$ ]]; then
            echo "ABBRUCH: Tag '$TAG' ist kein <component>-v<SemVer>." >&2; exit 1
          fi
          tag_component="${BASH_REMATCH[1]}"
          tag_version="${BASH_REMATCH[2]}"
          # 2. Erwarteter Paketname der klassifizierten Komponente == Tag-Component
          #    (unscoped) UND 3. Tag-Version == package.json.version AM Ref.
          pkg_name=$(node -p "require('./$DIR/package.json').name")
          pkg_version=$(node -p "require('./$DIR/package.json').version")
          [ "${pkg_name##*/}" = "$tag_component" ] \
            || { echo "ABBRUCH: Tag-Component '$tag_component' != Paket '$pkg_name'." >&2; exit 1; }
          [ "$pkg_version" = "$tag_version" ] \
            || { echo "ABBRUCH: Tag-Version '$tag_version' != package.json '$pkg_version' am Ref." >&2; exit 1; }
          echo "ok: $TAG ↔ $pkg_name@$pkg_version"

      # KEIN version:-Input — packageManager in der package.json entscheidet.
      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'pnpm'

      - name: Install dependencies
        # Reproduzierbar: frozen lockfile, kein stiller Lockfile-Drift beim Publish.
        run: pnpm install --frozen-lockfile

      - name: Paket + Workspace-Abhaengigkeiten bauen
        env:
          DIR: ${{ needs.classify.outputs.dir }}
        run: |
          set -euo pipefail
          name=$(node -p "require('./$DIR/package.json').name")
          # "$name..." = das Paket UND seine Workspace-Deps. pnpm ueberspringt
          # Pakete ohne build-Script.
          pnpm --filter "${name}..." build

      # pnpm pack loest "workspace:*" auf die konkrete Version auf — npm-Paket und
      # Release-Asset sind damit byte-identisch.
      - name: Tarball packen
        id: pack
        run: |
          set -euo pipefail
          mkdir -p /tmp/tgz
          pnpm -C "${{ needs.classify.outputs.dir }}" pack --pack-destination /tmp/tgz
          tarball=$(ls /tmp/tgz/*.tgz)
          echo "tarball=$tarball" >> "$GITHUB_OUTPUT"
          ls -l "$tarball"

      - name: Upload tarballs to GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ github.event_name == 'release' && github.event.release.tag_name || inputs.tag }}
        run: gh release upload "$TAG" /tmp/tgz/*.tgz --clobber

      # Trusted Publishing verlangt npm >= 11.5.1. Explizit sicherstellen statt
      # hoffen: ein zu altes npm kennt den OIDC-Austausch nicht und scheitert mit
      # irrefuehrendem 404.
      - name: npm-Version für Trusted Publishing sicherstellen
        # Gepinnt statt @latest: reproduzierbar und >= 11.5.1 (OIDC/Trusted
        # Publishing). Bewusst anheben, wenn noetig — nicht unkontrolliert driften.
        run: |
          npm install -g npm@11.5.1
          npm --version

      # npm publish auf den fertigen Tarball (hat die workspace:*-Rewrites schon
      # drin). Drei-Fall-Auswertung von npm view, damit weder ein stiller Skip
      # noch ein maskierter Auth-Fehler passiert.
      - name: Publish to npm (Trusted Publishing via OIDC)
        env:
          DIR: ${{ needs.classify.outputs.dir }}
          TARBALL: ${{ steps.pack.outputs.tarball }}
        run: |
          set -euo pipefail
          name=$(node -p "require('./$DIR/package.json').name")
          version=$(node -p "require('./$DIR/package.json').version")

          set +e
          view_out=$(npm view "$name@$version" version 2>&1)
          view_rc=$?
          set -e

          if [ "$view_rc" -eq 0 ] && [ "$view_out" = "$version" ]; then
            echo "übersprungen: $name@$version liegt bereits auf npm"
            exit 0
          fi
          if [ "$view_rc" -ne 0 ] && ! printf '%s' "$view_out" | grep -q 'E404'; then
            echo "ABBRUCH: npm view für $name@$version fehlgeschlagen:" >&2
            printf '%s\n' "$view_out" >&2
            exit 1
          fi

          echo "publiziere $name@$version aus $TARBALL"
          npm publish "$TARBALL" --access public
