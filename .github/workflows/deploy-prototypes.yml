name: Deploy

on:
  push:
    branches:
      - master
    paths:
      - 'apps/**'
      - 'packages/**'
      - 'docs/handbook/**'
      - 'scripts/site/**'
      - 'llms.txt'
      - 'pnpm-workspace.yaml'
      - '.github/workflows/deploy-prototypes.yml'
  workflow_dispatch:
    inputs:
      rollback_tag:
        description: 'OTA rollback: release tag to restore (e.g. ota-a3f9c12). Leave empty for normal deploy.'
        required: false
        type: string

# Nur der Grundstock. `pages: write` und `id-token: write` braucht allein der
# `deploy`-Job und stehen darum dort — sonst erbt sie auch `build`, der
# `pnpm install` mit Lifecycle-Skripten und die Builds fährt.
permissions:
  contents: read

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    # `contents: write` nur für `gh release create` (OTA-Bundles). Das läuft
    # über `GH_TOKEN` als Env, nicht über die Git-Konfiguration — daher unten
    # `persist-credentials: false`.
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # Abhaengigkeiten der Apps, aus dem Workspace-Graphen abgeleitet statt
      # von Hand aufgezaehlt. Die fruehere Liste nannte data-interface,
      # toolkit und drei Connectoren — als `supabase-connector` dazukam
      # (#238), trug sie niemand nach, und der Deploy brach ab dem naechsten
      # Push ab: `Cannot find module '@real-life-stack/supabase-connector'`.
      # Eine Handliste driftet lautlos; `^...` kann das nicht.
      - name: Build workspace dependencies
        run: pnpm --filter "reference^..." build

      - name: Set SHORT_SHA
        run: echo "SHORT_SHA=$(git rev-parse --short HEAD)" >> $GITHUB_ENV

      # OTA bundles MUST be built with base path "/" — the native app
      # loads them from capacitor://localhost/ (and the embedded bundle
      # from `cap sync` is also base "/"). Building them with "/app/"
      # (the web deployment base) makes every asset 404 after an OTA
      # reload — a blank screen. The web-of-trust demo gets this right.
      #
      # Ein Build JE KANAL — das ist der Fix für #193 und der Grund, warum hier
      # bewusst dreimal gebaut wird.
      #
      # `live-update.ts` liest `VITE_UPDATE_CHANNEL` zur Build-Zeit ins Bundle.
      # Nach einem OTA-Reload läuft das HERUNTERGELADENE Bundle, nicht das
      # eingebaute — es kennt seinen Kanal also nur, wenn er beim Bau gesetzt
      # war. Ohne ihn greift der Fallback `Capacitor.getPlatform()`.
      #
      # Der ist für zwei von drei Kanälen zufällig richtig, weil `ios` und
      # `android` wie ihre Plattform heissen. `android-foss` nicht: eine
      # F-Droid-Installation landete damit auf dem Play-Kanal. Deshalb
      # bekommt jeder Kanal seinen eigenen Build statt sich auf die
      # Namensgleichheit zu verlassen.
      # Pro Kanal ein eigener Build: VITE_UPDATE_CHANNEL landet im Bundle.
      # Bewusst ohne turbo-Cache — ohne die Variable im Cache-Schluessel
      # bekaemen alle drei Kanaele das Bundle des ersten, genau der Fehler
      # aus #193.
      - name: Build OTA bundles
        run: |
          for channel in ios android android-foss; do
            VITE_UPDATE_CHANNEL="$channel" pnpm --filter reference build
            (cd apps/reference/dist && zip -r "../../../bundle-${channel}-${SHORT_SHA}.zip" .)
          done
        env:
          VITE_BASE_PATH: /
          VITE_UPDATE_SERVER_URL: https://real-life-stack.de

      # Sentinel wie in scripts/release/build-android.sh: der String
      # 'android-foss' kommt NUR über VITE_UPDATE_CHANNEL ins Bundle. Er MUSS
      # im foss-Archiv stehen und DARF NICHT in den beiden anderen — das fängt
      # sowohl den verlorenen Kanal (#193) als auch vertauschte Archive.
      #
      # Nur dieser eine String wird geprüft: 'ios' und 'android' sind zu kurz
      # und kommen auch sonst im Bundle vor, eine Positiv-Prüfung darauf wäre
      # wertlos. Ausserdem faltet der Minifier den Fallback weg, sobald der
      # Kanal konstant ist — greppbar bleibt zuverlässig nur der Kanalname
      # selbst.
      #
      # Geprüft wird ausschliesslich `assets/*.js`, exakt wie `verify_bundle()`
      # im Release-Skript (`grep -rlq "$OTA_SENTINEL" "$d"/*.js`). Über das
      # ganze Archiv zu greppen würde eine statische HTML-, JSON- oder
      # SVG-Datei mit demselben String genügen lassen — der Kanal muss aber im
      # ausführbaren JS stehen, sonst nützt er zur Laufzeit nichts.
      - name: Verify OTA channel sentinel
        run: |
          set -euo pipefail
          for channel in ios android android-foss; do
            zip="bundle-${channel}-${SHORT_SHA}.zip"
            rm -rf /tmp/sentinel && mkdir -p /tmp/sentinel
            unzip -q "$zip" -d /tmp/sentinel
            ls /tmp/sentinel/assets/*.js >/dev/null 2>&1 \
              || { echo "FEHLER: $zip enthaelt kein assets/*.js" >&2; exit 1; }
            if grep -lqF 'android-foss' /tmp/sentinel/assets/*.js; then found=yes; else found=no; fi
            if [ "$channel" = android-foss ] && [ "$found" != yes ]; then
              echo "FEHLER: $zip enthaelt seinen Kanal 'android-foss' nicht (#193)" >&2
              exit 1
            fi
            if [ "$channel" != android-foss ] && [ "$found" = yes ]; then
              echo "FEHLER: $zip enthaelt 'android-foss' — vertauschtes Archiv?" >&2
              exit 1
            fi
            echo "ok: $zip"
          done

      # The Pages build is served at real-life-stack.de/app/, so it
      # keeps base "/app/".
      - name: Build reference app (for Pages)
        run: pnpm --filter reference build
        env:
          VITE_BASE_PATH: /app/
          VITE_UPDATE_SERVER_URL: https://real-life-stack.de
          VITE_UPDATE_CHANNEL: ios

      - name: Build prototype
        working-directory: apps/prototype
        run: pnpm tsc -b && pnpm vite build --base="/edge/"

      - name: Build Storybook
        run: pnpm build:storybook

      # Die Site (Landing + Handbuch) nach dem Storybook: ihr Pruefschritt
      # liest den Storybook-Index, damit jede eingebettete Story existiert.
      - name: Build site (landing + handbook)
        run: pnpm build:site && pnpm check:site

      - name: Compute bundle hashes
        run: |
          for channel in ios android android-foss; do
            sha256sum "bundle-${channel}-${SHORT_SHA}.zip" | cut -d' ' -f1 > "hash-${channel}.txt"
          done

      - name: Create GitHub Release with OTA bundles
        if: inputs.rollback_tag == ''
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "ota-${SHORT_SHA}" \
            --title "OTA ${SHORT_SHA}" \
            --notes "Automatic OTA release from commit ${SHORT_SHA}" \
            bundle-ios-${SHORT_SHA}.zip \
            bundle-android-${SHORT_SHA}.zip \
            bundle-android-foss-${SHORT_SHA}.zip

      - name: Generate latest.json (normal deploy)
        if: inputs.rollback_tag == ''
        run: |
          REPO="${{ github.repository }}"
          for channel in ios android android-foss; do
            HASH=$(cat "hash-${channel}.txt")
            mkdir -p gh-pages-ota/updates/${channel}
            printf '{"bundleId":"%s","url":"%s","sha256":"%s"}\n' \
              "${SHORT_SHA}" \
              "https://github.com/${REPO}/releases/download/ota-${SHORT_SHA}/bundle-${channel}-${SHORT_SHA}.zip" \
              "${HASH}" \
              > gh-pages-ota/updates/${channel}/latest.json
          done

      - name: Generate latest.json (rollback)
        if: inputs.rollback_tag != ''
        # inputs.rollback_tag NIE direkt in run: interpolieren — das waere
        # Script-Injection. Ueber env ist der Wert ein inertes Datum, das
        # Skript quotet ihn. Gleiche Regel wie in build-on-tag.yml.
        env:
          GH_TOKEN: ${{ github.token }}
          ROLLBACK_TAG: ${{ inputs.rollback_tag }}
        run: |
          REPO="${{ github.repository }}"
          TAG="$ROLLBACK_TAG"
          BUNDLE_ID="${TAG#ota-}"
          for channel in ios android android-foss; do
            mkdir -p gh-pages-ota/updates/${channel}
            # Download bundle to compute hash
            gh release download "${TAG}" -p "bundle-${channel}-${BUNDLE_ID}.zip" -D /tmp
            HASH=$(sha256sum "/tmp/bundle-${channel}-${BUNDLE_ID}.zip" | cut -d' ' -f1)
            printf '{"bundleId":"%s","url":"%s","sha256":"%s"}\n' \
              "${BUNDLE_ID}" \
              "https://github.com/${REPO}/releases/download/${TAG}/bundle-${channel}-${BUNDLE_ID}.zip" \
              "${HASH}" \
              > gh-pages-ota/updates/${channel}/latest.json
          done
          echo "Rolling back OTA to ${TAG}"

      - name: Prepare deployment
        run: |
          mkdir -p gh-pages/app gh-pages/edge gh-pages/storybook
          cp -R apps/site/dist/. gh-pages/
          cp -R apps/reference/dist/. gh-pages/app/
          cp apps/reference/public/404.html gh-pages/404.html
          cp -R apps/prototype/dist/. gh-pages/edge/
          cp -R packages/toolkit/storybook-static/. gh-pages/storybook/
          cp -R gh-pages-ota/updates/. gh-pages/updates/

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: gh-pages

  deploy:
    runs-on: ubuntu-latest
    needs: build
    # `actions/deploy-pages` braucht genau diese beiden; der Job checkt nichts
    # aus und führt keinen Projektcode aus.
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
