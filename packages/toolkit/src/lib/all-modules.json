{
  "$comment": "Generated by scripts/modules/generate.mjs from the module register and the hint table. Do not edit; run pnpm docs:modules.",
  "modules": [
    {
      "id": "feed",
      "label": "Feed",
      "icon": "Newspaper",
      "view": "FeedModule",
      "enabledByDefault": true,
      "fill": "container",
      "panelFit": "inset",
      "maxWidth": "max-w-3xl",
      "keepMounted": false,
      "presents": [],
      "loads": "host",
      "options": {
        "suggestType": "post",
        "createShell": "fullscreen"
      },
      "hints": [],
      "story": "rls-modules-feed--docs",
      "storyFile": "packages/toolkit/src/components/feed/feed-module.stories.tsx",
      "spec": "docs/spec/modules/feed.md",
      "intro": "**The feed** is the aggregating view of a space: everything that stands as a card of its own — posts, events, tasks, places — newest first, with comments and reactions on the card. It is a toolkit module and runs inside the module host (spec 01): this story contains no line of feed wiring."
    },
    {
      "id": "kanban",
      "label": "Kanban",
      "icon": "Columns3",
      "view": "KanbanModule",
      "enabledByDefault": true,
      "fill": "container",
      "panelFit": "inset",
      "maxWidth": "max-w-5xl",
      "keepMounted": false,
      "presents": [
        "status"
      ],
      "loads": "host",
      "options": {
        "suggestType": "task"
      },
      "hints": [
        {
          "name": "status",
          "key": "hasStatus",
          "filter": "{ hasField: [o.statusField ?? \"status\"] }"
        }
      ],
      "story": "rls-modules-kanban--docs",
      "storyFile": "packages/toolkit/src/components/kanban/kanban-module.stories.tsx",
      "spec": "docs/spec/modules/kanban.md",
      "intro": "**The kanban** shows the tasks of a space in columns by status and lets you drag them. It is a toolkit module and runs inside the module host (spec 01): the host loads what carries a status (`presents: [\"status\"]` — the field decides, never the type), applies search and filter, and provides detail, create (\"Aufgabe\" suggested) and the plus button."
    },
    {
      "id": "calendar",
      "label": "Kalender",
      "icon": "Calendar",
      "view": "CalendarModule",
      "enabledByDefault": true,
      "fill": "container",
      "panelFit": "inset",
      "maxWidth": "max-w-5xl",
      "keepMounted": false,
      "presents": [
        "start"
      ],
      "loads": "host",
      "options": {
        "suggestType": "event"
      },
      "hints": [
        {
          "name": "start",
          "key": "hasStart",
          "filter": "{ hasField: [\"start\"] }"
        }
      ],
      "story": "rls-modules-calendar--docs",
      "storyFile": "packages/toolkit/src/components/calendar/calendar-module.stories.tsx",
      "spec": "docs/spec/modules/calendar.md",
      "intro": "**The calendar** shows the events of a space by month, week, day or as a list. It is a toolkit module and runs inside the module host (spec 01): the host loads what carries a start (`presents: [\"start\"]` — a task with a date appears too), applies search and filter, and provides detail, create (\"Termin\" suggested) and the plus button."
    },
    {
      "id": "map",
      "label": "Karte",
      "icon": "MapIcon",
      "view": "MapModule",
      "enabledByDefault": true,
      "fill": "bleed",
      "panelFit": "overlay",
      "maxWidth": null,
      "keepMounted": true,
      "presents": [
        "position"
      ],
      "loads": "module",
      "options": {
        "suggestType": "place"
      },
      "hints": [
        {
          "name": "position",
          "key": "hasPosition",
          "filter": "{ hasField: [\"position\"] }"
        }
      ],
      "story": "rls-modules-map--docs",
      "storyFile": "packages/toolkit/src/modules/map-module.stories.tsx",
      "spec": "docs/spec/modules/map.md",
      "intro": "**The map** shows the items of a space that have a position, clustered, with the shared search floating over it. It is a toolkit module and runs inside the module host (spec 01) — with one difference: it **loads itself** (`loads: \"module\"`), by viewport, so a large space never loads its whole stock. The host issues no query but still provides detail (without backdrop, `panelFit: \"overlay\"`, so the map stays movable), create (\"Ort\" suggested) and the plus button. Picking a location for a new place happens on this map: the panel steps aside while you pick."
    },
    {
      "id": "resonance",
      "label": "Resonanz",
      "icon": "Waves",
      "view": "ResonanceModule",
      "enabledByDefault": false,
      "fill": "container",
      "panelFit": "inset",
      "maxWidth": "max-w-3xl",
      "keepMounted": false,
      "presents": [
        "statement"
      ],
      "loads": "host",
      "options": {
        "suggestType": "statement"
      },
      "hints": [
        {
          "name": "statement",
          "key": "hasStatement",
          "filter": "{ type: [...typesWithAffordance(\"votesOn\", \"to\")] }"
        }
      ],
      "story": "rls-modules-resonance--docs",
      "storyFile": "packages/toolkit/src/modules/resonance-module.stories.tsx",
      "spec": "docs/spec/modules/resonance.md",
      "intro": "**Resonance** collects statements a group positions itself on: each card carries a vote bar, and the module sorts by resonance. It is a toolkit module and runs inside the module host (spec 01): the host loads the class `statement` (a hint by affordance, not by field — spec 06), applies search and filter, and provides detail, create (\"Aussage\" suggested) and the plus button."
    },
    {
      "id": "collection",
      "label": "Liste",
      "icon": "List",
      "view": "CollectionModule",
      "enabledByDefault": false,
      "fill": "bleed",
      "panelFit": "inset",
      "maxWidth": "max-w-6xl",
      "keepMounted": false,
      "presents": [],
      "loads": "host",
      "options": {},
      "hints": [],
      "story": "rls-modules-list--docs",
      "storyFile": "packages/toolkit/src/modules/collection-module.stories.tsx",
      "spec": null,
      "intro": "**The list** shows everything in a space that stands as a card of its own, as a list or a grid; the density toggle sits in the header next to the search. It is a toolkit module and runs inside the module host (spec 01) without a line of wiring here: the host loads (no `presents` — the list aggregates), filters, and provides detail, create and the plus button."
    },
    {
      "id": "graph",
      "label": "Graph",
      "icon": "Share2",
      "view": "GraphModule",
      "enabledByDefault": false,
      "fill": "bleed",
      "panelFit": "overlay",
      "maxWidth": null,
      "keepMounted": false,
      "presents": [],
      "loads": "host",
      "options": {},
      "hints": [],
      "story": "rls-modules-graph--docs",
      "storyFile": "packages/toolkit/src/modules/graph-module.stories.tsx",
      "spec": null,
      "intro": "**The graph** shows the items of a space and their relations as a force layout: embedded relations (a task assigned to a person) and relation records (a vote on a statement) become edges; people appear as nodes only when an edge reaches them. It is a toolkit module and runs inside the module host (spec 01): the host loads everything that stands as a card, applies search and filter — here floating over the canvas (`panelFit: \"overlay\"`) — and provides detail, create and the plus button."
    }
  ]
}
